# jobquip.com 代码级深度审计报告

> 审计时间: 2026-04-21 00:21
> 审计范围: GitHub 代码库 (Hhezhiqiang/jobs-platform) + Vercel 线上部署实测
> 框架: Next.js (App Router) + Prisma + Vercel 部署

---

## 一、线上实测验证结果

### 1.1 职位列表页 `/zh/jobs` — 实际不是空的 ✅ 但有隐患

通过 `curl` 抓取线上 HTML，发现职位列表页**实际上有 SSR 渲染**：
- 20 条职位数据通过 RSC payload 嵌入 HTML 中
- 服务器端调用了 Prisma 查询并传入了 `JobsPageClient`

**但是**，这是 Next.js RSC Streaming 模式，HTML 中职位数据被编码为 RSC payload（`self.__next_f.push(...)`），不是传统 SSR 的直接 HTML。Google 爬虫虽然能执行 JS，但对 RSC payload 的解析不如直接 HTML 可靠。

**结论**: 不是"完全为空"，但搜索引擎友好度有优化空间。

### 1.2 英文页面中文混杂 — 严重 ❌

线上实测 `/en` 页面，HTML 中包含：
- "职位" 出现 **62 次**
- "公司" 出现 **42 次**
- "薪资" 出现 **11 次**
- "注册" 出现 **8 次**
- "常见问题" 出现 **2 次**
- 导航栏、Footer、CTA 全部是中文

**根因**: 国际化翻译不完整。i18n 翻译文件中只有部分 key 有英文版本，大量硬编码中文文本未走 i18n。

### 1.3 首页标题关键词堆砌

线上 `/zh` 页面标题：
```
JobQuip招聘平台 - 专业求职招聘平台，汇聚Web3、互联网高薪职位
```
关键词密度过高，且 title 模板中重复了"招聘平台"。

---

## 二、逐问题代码级分析

### 🔴 P0 — 必须立即修复

#### 1. 数据不一致 — 虚假统计数字

| 位置 | 数据 | 真实数据（数据库） |
|------|------|-------------------|
| HeroSection | `{jobCount}+ 职位发布` | **42** |
| StatsSection | `98% 简历通过率` | **硬编码，假数据** |
| About Page | `10,000+ 优质职位、500+ 合作企业、50,000+ 注册用户` | **全部硬编码，与数据库差几百倍** |

**影响**: 
- Google E-E-A-T 严重受损
- 用户信任度为零（看到 42 个职位却说 10,000+）
- 可能被判定为欺骗性内容

**修复方案**: 
- About 页的统计数据改为从数据库查询真实值
- 如果真实数字太小，暂时移除这些统计，而不是用假数字

#### 2. 英文页面中文混杂

**涉及文件**:
- `src/components/header.tsx` — 导航栏硬编码中文
- `src/components/footer.tsx` — 底部硬编码中文
- `src/components/hero-section.tsx` — 部分中文
- `src/components/stats-section.tsx` — 全中文
- `src/components/features-section.tsx` — 全中文
- `src/app/[locale]/page.tsx` — 首页大量中文
- `src/app/[locale]/about/page.tsx` — 关于页全中文
- `src/app/[locale]/faq/page.tsx` — FAQ 全中文
- `src/app/[locale]/blog/page.tsx` — 博客页标题和描述中文
- i18n 翻译文件 `messages/en.json` — 翻译不完整

**修复方案**: 
- 所有组件的文本必须走 `useTranslations()` 或 server 端的 `getTranslations()`
- 完善 `messages/en.json`
- 英文页面使用独立 SEO metadata

#### 3. 博客内容质量风险

**发现**:
- `src/lib/auto-blog-generator.ts` — 使用 KIMI API 自动生成博客
- `src/lib/auto-publisher.ts` — 自动发布流水线
- 环境变量 `AUTO_PUBLISH_ENABLED` 控制开关

**风险**:
- 如果自动生成的内容质量低（关键词堆砌、内容空洞），Google 2024 年 3 月核心更新会打击这类内容
- 代码中的 prompt 要求"深度原创"，但最终质量取决于 AI 输出
- 建议：上线前人工审核每篇自动生成的博客

### 🟠 P1 — 高价值修复

#### 4. 结构化数据 — 部分已有，但缺关键场景

**已有的**:
- ✅ `JobPosting` Schema — 职位详情页 (`src/app/[locale]/jobs/[slug]/page.tsx`)
- ✅ `Organization` Schema — layout.tsx 头部注入
- ✅ `WebSite` Schema — layout.tsx 头部注入
- ✅ `FAQPage` Schema — 首页
- ✅ `BreadcrumbList` Schema — 职位详情页

**缺失的**:
- ❌ `JobPosting` Schema **没有**出现在职位列表页
- ❌ `ItemList` Schema **没有**出现在职位列表页/公司列表页
- ❌ `FAQPage` Schema **没有**出现在 FAQ 页面 (`src/app/[locale]/faq/page.tsx`)
- ❌ `Organization` Schema 在公司详情页没有注入

**修复**: 在对应页面注入相应的 Schema。

#### 5. Sitemap 时间戳问题

`src/app/sitemap.ts` 中：
```typescript
{ url: `${zh}`, priority: 1.0, changeFrequency: "daily", lastModified: new Date() }
```

静态页面使用 `new Date()`（每次请求都是当前时间），动态条目（博客、职位）使用数据库的 `updatedAt`（正确）。

**影响**: 静态页面的 lastMod 每次都变，搜索引擎无法区分真实更新。

**修复**: 静态页面使用固定的构建时间或缓存的时间。

#### 6. Hreflang — 已配置 ✅

代码中已正确配置：
```typescript
alternates: {
  languages: {
    "zh-CN": `${SITE_URL}/zh`,
    "en": `${SITE_URL}/en`,
    "x-default": `${SITE_URL}/zh`,
  },
}
```

每个页面都有 alternates 配置。✅ 不是问题。

#### 7. Meta Title/Description — 部分可优化

**首页标题过长**:
```
JobQuip招聘平台 - 专业求职招聘平台，汇聚Web3、互联网高薪职位
```
"招聘平台"重复了。

**职位列表页标题**:
```
招聘信息 - 最新职位列表 | JobQuip招聘平台
```
可以更精准，例如：`最新Web3与互联网招聘职位 | JobQuip`

**建议**: 简化标题，减少关键词重复，增加可读性。

### 🟡 P2 — 中优先级

#### 8. 职位详情页内容丰富度

**已有**:
- 职位描述、任职要求、福利待遇（来自数据库）
- 公司信息卡片
- FAQ Schema 自动生成
- 申请按钮
- 面试经验 Tab（如果有数据）

**缺失**:
- 很多职位的 description/requirements/benefits 字段很短或为空
- 这是数据问题，不是代码问题 — 需要运营录入更详细的 JD

#### 9. 关于页团队信息

团队信息是硬编码的中文名 + 首字母头像：
```typescript
const team = [
  { name: "陈明远", role: "创始人 & CEO", ... },
  { name: "赵文博", role: "技术总监", ... },
  { name: "林思琪", role: "运营总监", ... },
];
```

**建议**: 
- 如果是真实团队，加 LinkedIn 链接和真实照片
- 如果是占位信息，建议先移除，比虚假的好

#### 10. 图片 alt 属性

公司 logo 使用 `ui-avatars.com` 生成，但 `next/image` 组件中：
```tsx
<Image src={company.logo} alt={`${company.name} 公司Logo`} ... />
```
已有 alt 属性。✅

但首页 Hero 区域的背景装饰图片没有 alt（因为是 CSS 背景，不是 img 标签）。✅ 这不是问题。

### 🟢 P3 — 低优先级

#### 11. URL 结构

当前 slug 格式：`trust-wallet-senior-product-manager-1775816724310-17`

包含时间戳和数字 ID。代码中 slug 是由系统自动生成的（看起来是在数据导入时拼接的）。

**建议**: 清理为 `trust-wallet-senior-product-manager`。但这需要数据库迁移和重定向配置，成本较高。

#### 12. Open Graph / Twitter Card — 已有 ✅

`src/app/[locale]/layout.tsx` 已配置：
```typescript
openGraph: { title, description, url, siteName, type, images },
twitter: { card, title, description, images },
```

✅ 不是问题。

#### 13. robots.txt — 配置正确 ✅

`src/app/robots.ts` 配置合理：
- 允许所有搜索引擎抓取公开页面
- 禁止 admin、api、dashboard 等私有路径
- 专门配置了 AI 爬虫（GPTBot、CCBot 等）

✅ 不是问题。

---

## 三、线上部署状态

| 项目 | 状态 |
|------|------|
| Vercel 项目 | ✅ prj_HNhsb7UKdYpXzES4h8GmVo7VRNFt |
| Git Remote | ✅ Hhezhiqiang/jobs-platform (main) |
| 本地代码 | ✅ 与远端同步 (Already up to date) |
| 框架 | Next.js App Router |
| 数据库 | Prisma (外部数据库) |

---

## 四、修复优先级清单

| 优先级 | 问题 | 预估工时 | 影响 |
|--------|------|----------|------|
| **P0** | 修复 About 页虚假统计数据 | 0.5h | 信任度/E-E-A-T |
| **P0** | 修复 StatsSection 硬编码 98% | 0.5h | 信任度 |
| **P0** | 英文页面全中文化 | 2-3h | 英文搜索流量归零 |
| **P1** | 简化首页标题 | 0.5h | CTR |
| **P1** | 职位列表页添加 ItemList Schema | 1h | 富媒体展示 |
| **P1** | FAQ 页面添加 FAQPage Schema | 0.5h | 富媒体展示 |
| **P1** | 修复 Sitemap 静态页时间戳 | 0.5h | 爬虫效率 |
| **P2** | 博客内容人工审核机制 | 1h | 避免算法惩罚 |
| **P2** | 关于页团队信息处理 | 0.5h | 信任度 |
| **P3** | URL slug 清理 | 2-3h | 用户体验 |

---

## 五、代码中发现的其他问题

### 5.1 Google Analytics 未配置

线上 HTML 中：
```
https://www.googletagmanager.com/gtag/js?id=G-PLACEHOLDER_REPLACE_ME
```
GA ID 还是占位符，意味着**没有流量统计**。

### 5.2 百度推送脚本加载

页面中嵌入了百度主动推送脚本：
```js
bp.src = 'https://zz.bdstatic.com/linksubmit/push.js';
```
这在 Google 索引的站点上没有必要，还可能拖慢页面加载。

### 5.3 搜索引擎验证占位符

```html
<meta name="sogou_site_verification" content="your-code"/>
<meta name="360-site-verification" content="your-code"/>
```
搜狗和 360 的验证码还是 "your-code"，说明没有实际提交到这些搜索引擎。

### 5.4 百度验证已通过

```html
<meta name="baidu-site-verification" content="code-wV5u95unUV"/>
```
这个有实际值，说明百度已验证。

### 5.5 中间件强制登录问题

`src/middleware.ts` 中，未登录用户会被重定向到登录页（除了少数公开路径）。这意味着：
- `/zh/jobs` — 公开 ✅（不在受限路径中）
- `/zh/blog` — 公开 ✅
- `/zh/companies` — 公开 ✅

但 dashboard、admin、company 等路径需要登录。这是正确的设计。

---

## 六、总结

JobQuip 的代码质量**总体不错**——架构清晰，SEO 意识已经很强（有 Schema、OG、Sitemap、Hreflang、robots.txt）。

**最该修的不是技术问题，而是内容真实性问题**：
1. 假统计数据 → 用真实数据或移除
2. 英文页面中文 → 完整翻译
3. 博客自动发布 → 加人工审核
4. GA 未配置 → 先装上才能看到 SEO 效果

把这些搞定，SEO 基础就扎实了。
