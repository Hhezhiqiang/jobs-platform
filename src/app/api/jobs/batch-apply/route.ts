import { NextRequest, NextResponse } from "next/server";import { getServerSession } from "next-auth/next";import { authOptions } from "@/lib/auth";import { prisma } from "@/lib/prisma";export const dynamic = "force-dynamic";export async function POST(request: NextRequest) {try {const session = await getServerSession(authOptions);const body = await request.json();const { jobIds, coverLetter, email, name } = body;if (!jobIds || !Array.isArray(jobIds) || jobIds.length === 0) return NextResponse.json({ error: "请选择至少一个岗位" }, { status: 400 });const userId = session?.user?.id || null;const isGuest = !userId;if (isGuest && !email) return NextResponse.json({ error: "请填写邮箱" }, { status: 400 });let success = 0, skipped = 0;for (const jobId of jobIds) {try {const existing = await prisma.job_applications.findFirst({ where: { jobId, OR: [{ userId: userId || undefined }, ...(isGuest ? [{ guestEmail: email }] : [])] } });if (existing) { skipped++; continue; }await prisma.job_applications.create({ data: { jobId, userId: userId || undefined, guestEmail: isGuest ? email : undefined, guestName: isGuest ? name || undefined : undefined, coverLetter: coverLetter || undefined, status: "PENDING" } });success++;} catch (e) { skipped++; }}return NextResponse.json({ success: true, applied: success, skipped, total: jobIds.length });} catch (error) { return NextResponse.json({ error: "失败" }, { status: 500 }); }}