# 简历上传功能实现文档

## 功能概述

为 /dashboard/profile 页面添加了完整的简历上传和管理功能，支持 PDF、DOC、DOCX 格式文件上传，文件大小限制 10MB。

## 创建/修改的文件列表

### 1. API 路由

| 文件路径 | 说明 |
|---------|------|
| `src/app/api/resumes/upload/route.ts` | 简历上传 API，处理文件上传和数据库保存 |
| `src/app/api/resumes/route.ts` | 获取当前用户的所有简历列表 |
| `src/app/api/resumes/[id]/route.ts` | 单条简历操作：获取、删除、更新（设置默认） |

### 2. 组件

| 文件路径 | 说明 |
|---------|------|
| `src/components/resume-upload.tsx` | 简历上传组件，包含拖拽上传、文件列表展示、操作按钮 |

### 3. 工具库

| 文件路径 | 说明 |
|---------|------|
| `src/lib/resume-parser.ts` | 简历解析工具，使用正则提取姓名、邮箱、手机号、技能等信息 |

### 4. 页面更新

| 文件路径 | 说明 |
|---------|------|
| `src/app/dashboard/profile/page.tsx` | 新增"简历文件" Tab，集成 ResumeUpload 组件 |

### 5. 配置更新

| 文件路径 | 说明 |
|---------|------|
| `.gitignore` | 添加 `/public/uploads` 忽略上传的文件 |

## 依赖安装

```bash
npm install busboy @types/busboy pdf-parse --save
```

## 实现的功能点

### 1. 文件上传
- ✅ 支持 PDF、DOC、DOCX 格式
- ✅ 文件大小限制 10MB
- ✅ 拖拽上传和点击上传两种方式
- ✅ 文件类型验证和大小验证
- ✅ 唯一文件名生成（UUID）避免冲突
- ✅ 上传到 `public/uploads/resumes` 目录

### 2. 简历列表展示
- ✅ 显示已上传的所有简历
- ✅ 按默认状态和上传时间排序
- ✅ 显示文件格式、大小、上传日期
- ✅ 标记默认简历

### 3. 简历操作
- ✅ **设置默认简历**：点击星形图标设为默认
- ✅ **删除简历**：删除文件和数据库记录，自动更新默认简历
- ✅ **下载简历**：点击下载按钮下载文件

### 4. 数据库集成
- ✅ 使用已有的 Resume Prisma 模型
- ✅ 关联 User 模型（userId）
- ✅ 第一份上传的简历自动设为默认

### 5. 简历解析（可选）
- ✅ 基础 PDF 文本解析
- ✅ 正则提取：姓名、邮箱、手机号、技能、教育背景、工作经历
- ✅ 支持常见技术关键词匹配

## API 端点

### POST /api/resumes/upload
上传新简历

**请求**: `multipart/form-data`
- `file`: 文件（必需）
- `name`: 简历名称（可选，默认为文件名）

**响应**:
```json
{
  "success": true,
  "resume": {
    "id": "...",
    "name": "...",
    "fileUrl": "/uploads/resumes/xxx.pdf",
    "fileType": "application/pdf",
    "fileSize": 12345,
    "isDefault": true,
    "createdAt": "..."
  }
}
```

### GET /api/resumes
获取当前用户的所有简历

**响应**:
```json
{
  "resumes": [...]
}
```

### GET /api/resumes/[id]
获取单条简历详情

### DELETE /api/resumes/[id]
删除简历（同时删除文件和数据库记录）

### PATCH /api/resumes/[id]
更新简历信息

**请求体**:
```json
{
  "isDefault": true,
  "name": "新名称"
}
```

## 目录结构

```
jobs-platform/
├── public/
│   └── uploads/
│       └── resumes/          # 简历文件存储目录
├── src/
│   ├── app/
│   │   ├── api/
│   │   │   └── resumes/
│   │   │       ├── upload/
│   │   │       │   └── route.ts
│   │   │       ├── [id]/
│   │   │       │   └── route.ts
│   │   │       └── route.ts
│   │   └── dashboard/
│   │       └── profile/
│   │           └── page.tsx  # 已更新
│   ├── components/
│   │   └── resume-upload.tsx # 新组件
│   └── lib/
│       └── resume-parser.ts  # 解析工具
├── prisma/
│   └── schema.prisma         # Resume 模型已存在
└── package.json
```

## 测试建议

### 1. 功能测试

#### 上传功能
1. 上传 PDF 文件（< 10MB）- 期望：上传成功，显示在列表中
2. 上传 DOC 文件 - 期望：上传成功
3. 上传 DOCX 文件 - 期望：上传成功
4. 上传大于 10MB 的文件 - 期望：提示文件过大
5. 上传非支持格式（如 PNG）- 期望：提示格式不支持
6. 拖拽文件到上传区域 - 期望：触发上传

#### 列表功能
1. 上传多份简历 - 期望：按时间倒序排列
2. 查看文件信息 - 期望：显示格式、大小、日期

#### 操作功能
1. 设置默认简历 - 期望：该简历标记为默认，其他取消默认
2. 删除默认简历 - 期望：删除成功，最新简历自动设为默认
3. 删除非默认简历 - 期望：删除成功，默认简历不变
4. 下载简历 - 期望：文件正常下载

### 2. 边界测试

1. 空文件上传
2. 特殊字符文件名
3. 并发上传多个文件
4. 未登录状态下访问 API - 期望：401 错误

### 3. 集成测试

1. 上传简历后刷新页面 - 期望：列表正常显示
2. 上传后查看数据库 - 期望：Resume 表有正确记录
3. 检查文件系统 - 期望：文件保存在正确位置

### 4. 安全测试

1. 尝试上传可执行文件 - 期望：被拒绝
2. 尝试路径遍历攻击（文件名包含 `../`）- 期望：被处理或拒绝
3. 尝试访问他人简历 - 期望：404 错误

## 后续优化建议

1. **云存储支持**：将本地存储迁移到 AWS S3、阿里云 OSS 等
2. **简历解析增强**：使用 AI 或专业 NLP 库提取更丰富的信息
3. **简历预览**：支持在线预览 PDF，无需下载
4. **版本管理**：支持简历版本历史和回滚
5. **隐私设置**：控制简历的可见范围
6. **申请关联**：在投递职位时选择使用的简历
